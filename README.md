# Ariadne

**The collaborative AI harness — a knowledge OS for the AI era.**

*"Life takes 42 shots."*

AI runs the loops. Humans steer. AI learns. `Ariadne` forms a base of all my tinkering, it represents a paradigm of working. To adapt it to a new repo, from the target repo (assuming clone as sibling), run `../ariadne/construct/setup.sh`. 

Check `atlas/workflow/index.md` for how to use it (TODO).
