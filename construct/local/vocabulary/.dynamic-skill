#!/bin/sh
# weave-output: argv1
# weave supplies an isolated output directory as argv1 before publishing through
# ownership checks. Cwd remains the consuming repo for layer-graph reads.
# Direct invocation retains the conventional output directory.
vocabulary export --output "${1:-construct/generated/vocabulary}"
