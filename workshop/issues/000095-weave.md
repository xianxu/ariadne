---
id: 000095
status: working
deps: []
github_issue:
created: 2026-06-14
updated: 2026-06-14
estimate_hours: 28
---

# weave — intent compiler for layer composition (replaces construct/setup.sh)

## Problem

`construct/setup.sh` began as a run-once repo bootstrapper and has grown into the de-facto compiler that wires every ariadne-derived repo's agentic context. Three problems:

1. **Wrong altitude.** It's a bash interpreter of *file-ops* (`symlink X Y`) when what we want is a compiler of *intents* (`prose`, `skill`, `settings`, …). Structured composition — DAG resolution, JSON merge, multi-target codegen — strains bash, already evidenced by the separate `merge-settings.sh` and the bolted-on `discover-ancestors.test.sh` seam.
2. **Known bugs.** (a) Layer dependencies are partly carried by `go.mod` `replace` — the nous→brain *layer* edge lives in `go.mod`, not `construct/deps` (a constant source of confusion). (b) `@AGENTS.local.md` resolves through the symlinked `AGENTS.md` to *ariadne's* copy, so a downstream repo's own local prose never loads — every repo silently inherits ariadne's "you're in ariadne" note.
3. **Misleading name.** "setup" implies run-once; it's idempotent and should run on every layer/prose change.

## Spec

Replace `construct/setup.sh` with **`cmd/weave`** (Go, ariadne-owned) — a clean-sheet rewrite informed by 1000+ changes of experience, **not** a strangler port. Named `weave` for Ariadne's thread: it braids per-layer strands into one fabric.

**weave is dual-natured:**
- *Build-time compiler* — lowers each layer's declared **intents** into the target harness's runtime artifacts.
- *Run-time skill server* — `weave skills` (menu) / `weave skill <name>` (body) serve agent skills directly over shell, with no reliance on `.claude/skills/` discovery.

**Intent vocabulary** — a **hybrid** (the real `base.manifest` is ~all generic infrastructure verbs): weave **ports the existing file-op verbs** (`symlink`, `seed`, `scaffold`, `touch`, `merge`, `tool`) and **adds new semantic intents** (`prose`, `skill`). `merge` *is* the settings cascade; `prose` replaces the `@AGENTS.local.md` @-import; `skill` unifies the `sync-local-skills.sh` hook + per-layer skill symlinks. So #95 *adds* semantic intents atop the ported file-ops — it does not replace file-ops wholesale.

**Uniform lowering model.** weave lowers *every* intent per backend; intents differ only in **backend coverage** — not in kind:
- `prose` → composed into the harness system-prompt file (`AGENTS.md`). **Universal.**
- `skill` → served by weave itself; menu (names+descriptions) compiled into `AGENTS.md` for always-on discovery, bodies via `weave skill <name>`. **Universal** (weave supplies the mechanism).
- `settings` → **passthrough** to the harness's own config surface (Claude `settings.json` today). The one intent weave *cannot* universalize, because shell-tightness knobs (permissions/hooks/approval) are intrinsically per-harness. Thin portable core (env, coarse approval) + per-backend passthrough for the rich controls. **Narrowest coverage.**
- `scaffold` → directory conventions (`workshop/`, `atlas/`, …).
- `tool` → Go-binary ownership / build-in-owner wiring.

**Dependency & resolution.**
- Layer edges come **solely** from `construct/deps`; weave never infers them from `go.mod` or `go list -m all`. A build-necessary `replace` **stays**; a discovery-only one is **migrated**. (Verified: brain's `replace nous` is vestigial *for the Go build* but **load-bearing for layer discovery** — it's the sole current source of the nous→brain edge, since brain's `construct/deps` names only ariadne. So removing it is a required edge-preserving migration: add `substrate ../nous` first, then drop the `replace`.)
- Transitive resolution: topo-sort foundation-first, dedup (each layer applied exactly once).
- **Diamonds are free**: clone-as-peer + symlink means one on-disk copy per layer, so a diamond D→{B,C}→A collapses to "apply each layer once." **No versioning** — deliberate trade: single copy = single version; can't run repo D on old-A while E is on new-A (that'd need vendoring, out of scope).

**Cascade / collision semantics** (diamonds surface collisions, not version skew):
- `prose` → additive, concatenated foundation-first.
- `settings` → deep-merge, specific-wins.
- `skill`/files → namespaced (`xx-`/`nous-`/`metis-`) so they structurally can't collide.
- Suppressing an inherited fragment → deferred to v2; v1 is additive-only.

**Scope (v1).**
- Build the **`AGENTS.md` floor backend** + a clean backend-interface seam. Defer `.claude/rules` and native-skill backends. `settings.json` is v1's lone non-floor backend output (kept because it's load-bearing today — and it's the seam's first real client).
- Floor primitives assumed: a **system prompt + shell**. No `.claude/` assumptions anywhere in the core.
- **Symlink-only**; drop vendor mode (retire the stale `.ariadne-mode` marker).
- A thin shell bootstrap stub (the existing `bootstrap.sh`) builds + invokes `weave`.

**Open design details** (resolve in the plan): exact skill-menu placement (lean: descriptions compiled into `AGENTS.md`, bodies on demand); the extent of the portable settings core.

## Done when

- `cmd/weave` (Go) replaces `construct/setup.sh`; bootstrap/refresh invoke it; idempotent + re-runnable on any layer/prose change.
- **Golden-diff gate:** weave's output is diffed against `setup.sh`'s on the live repos (ariadne, nous, brain, metis); *every* divergence is accounted for in a reviewed ledger as an intended fix or a regression-then-fixed — **not** byte-equality (the current output contains the bugs above).
- A **fresh session** in a downstream repo (e.g. brain) loads *that repo's own* local prose, not ariadne's.
- Layer deps resolve **solely** from `construct/deps` (brain carries explicit `substrate ../nous`); `go.mod` no longer drives layer discovery.
- `weave skills` / `weave skill <name>` serve skills agent-agnostically (no `.claude/skills/` dependency).
- `settings.json` is still produced correctly for the Claude backend.
- Vendor mode removed; symlink-only.
- Tests: intent lowering, topo-sort + dedup (incl. a diamond fixture), cascade/merge semantics, and the golden-diff harness.

## Plan

> Detailed design → `workshop/plans/000095-weave-plan.md` (author via `superpowers-writing-plans` after `sdlc start-plan`). Provisional milestone sketch:

- [x] M1 — Design doc + dependency-model cutover decision (`construct/deps` as sole source)
- [x] M2 — Core: intent model + topo/dedup resolver + `prose` lowering (compose `AGENTS.md`) + golden-diff harness
- [x] M3 — `weave skills`/`weave skill` server + `scaffold` + `tool` intents
- [x] M4 — `settings` backend (settings.json) + backend-interface seam
- [ ] M5 — Cutover: replace `setup.sh`, migrate all repos, retire vendor mode; verify (golden-diff ledger + fresh-session prose check)

## Log

### 2026-06-15
- 2026-06-15: M5 side-quest — **dropped the go.mod `tool` lowering** (operator decision): the `tool` intent is FULLY removed (`intent.Tool` kind + parser case, the `ToolDep` action + `plan` lowering + `applyToolDep`, the `golden` `classifyToolDep` + completeness `tool` coverage + walk handling + all their tests; the now-dead `gomodx` pkg + the `weavefs.GoModEditor` exec seam deleted). Cross-repo grep (`*/construct/base.manifest`) found **only ariadne** carried `tool` rows → full-removal path (no parseable-no-op needed). Removed ariadne's `tool cmd/sdlc`/`tool cmd/weave` rows + `go mod edit -droptool`'d the stale `tool` directive from go.mod. Ownership is now location-based (`construct/dev-aliases.sh` + build-in-owner); deps via `weave link`/`construct/deps`. **New golden baseline: ariadne self-walk `golden --target claude` = MATCH 25, UNEXPECTED 0; the prior `tool go.mod` UNEXPECTED is GONE — intended divergences drop from 3 to 2 (AGENTS.md content + settings hook).** `make weave-build` still builds `bin/weave` (location-based, unaffected). go build/test/vet/gofmt clean; verify-complete = 0 under-production. Target `weave-composition-algebra.md` + atlas (`weave.md`, `setup-and-replication.md`) updated.

### 2026-06-14
- 2026-06-14: closed M4 — M4 = settings merge backend: pure settingsx.Merge ports merge-settings.sh exactly ($merge_keys union, $remove filter-before-union, meta-strip, local-overrides-base; cross-validated semantically vs the live bash). MergeSettings lowering + Apply (base + optional settings.local.json → settings.json); golden classifier compares semantically (merge retired from EXPECTED-deferred — only seed remains). Caught+fixed a gatherer symlinked-base bug. 115 tests GREEN, vet+gofmt clean. Live weave golden: merge MATCHES everywhere (semantic-equal to live settings.json); zero new UNEXPECTED (ariadne+metis clean; nous/brain still the 6 pre-existing stale-drift). No formal Backend interface (Action sum type is the seam; YAGNI, one backend). NO-ACTUAL: telemetry unmeasurable (brain session drives ariadne worktree).; review verdict: FIX-THEN-SHIP
- 2026-06-14: closed M3 — M3 = agent-agnostic skill server (SkillIndex pure + GatherSkills porting sync-local-skills.sh; menu compiled into AGENTS.md; weave skills/weave skill CLI) + tool intent lowering (bimodal: derivative→substrate row / owner→go mod edit -tool via injectable GoModEditor seam; golden classifier wired same-milestone) + weave link (directory-agnostic substrate). 90 tests GREEN, vet+gofmt clean. Live weave golden: tool now MATCHES everywhere (ariadne owner go.mod; nous/brain/metis derivative construct/deps); zero new UNEXPECTED (ariadne+metis clean; nous/brain still the 6 pre-existing stale-drift items). NO-ACTUAL: telemetry unmeasurable (brain session drives ariadne worktree).; review verdict: FIX-THEN-SHIP (judge re-run after a transient API socket error on the first dispatch)
- 2026-06-14: closed M2 — M2 = pure core (intent.ParseManifest, composeProse, Plan/Action) + IO seam (walk.Walk ports deps_substrate_targets + _seen_or_add + self-reference filters; idempotent Apply ports create_symlink/scaffold/touch; weave CLI + --dry-run) + golden-diff harness (pure classifier + weave golden). Full suite GREEN (intent/layer/plan/walk/golden + cmd), vet clean. Golden-diff vs live repos: ariadne-self + metis fully clean (0 UNEXPECTED); nous/brain 6 UNEXPECTED = pre-existing stale-derivative drift (#94 colima/vm-log manifest lines postdate their last setup; closed by re-weave at M5). Harness caught+fixed a Touch-clobbers-lessons.md bug. NO-ACTUAL: telemetry unmeasurable (brain session drives ariadne worktree).; review verdict: FIX-THEN-SHIP
- 2026-06-14: closed M1 — M1 = layer resolver + construct/deps parser. 7 unit tests green (resolver topo/diamond/cycle; parser substrate-row/file-order/ignore-data+comments+blanks/empty), go vet clean. Resolver ports discover_ancestors ordering; parser ports lib-deps.sh grammar (ARCH-DRY); both pure (ARCH-PURE). NO-ACTUAL: telemetry unmeasurable (brain session drives ariadne worktree, no correlated transcripts; recurs for all #95 milestones).
- Created from an extended brainstorm (conducted in the brain repo). Spec captures: weave = dual-natured intent compiler + skill server; uniform per-backend lowering (prose/skill universal, settings passthrough); `construct/deps` as sole layer-edge source; diamond-free single-copy model; symlink-only; `AGENTS.md` floor backend in v1.
- Triggering context: **metis** (a new ML base-layer repo, sibling of nous) needed per-repo charter prose, which surfaced (a) the `AGENTS.local.md` symlink-resolution bug and (b) the `go.mod`-vs-`construct/deps` layer-dependency confusion. weave generalizes the fix rather than patching metis.
- Flow: `sdlc start-plan` (ARCH-PURE/ARCH-DRY) → plan authored via `superpowers-writing-plans` (`workshop/plans/000095-weave-plan.md`) → `sdlc change-code` plan-quality judge returned **FAILURE** on three findings (intent vocab incomplete vs the generic-`symlink` manifest; imprecise `go.mod` dep-model with a stated regression risk; undefined `prose` grammar). All three verified against live source and addressed — intent vocab is now a hybrid (ported file-op verbs + new `prose`/`skill`), dep-model rule made precise, `prose <relpath>` grammar defined. See the plan's `## Revisions`.
- `change-code` re-run: VERDICT **INFO** (non-blocking) — worktree cut at `worktree/ariadne/000095-weave`. Two info findings folded in: (1) brain's `replace nous` is **load-bearing for layer discovery** (not "vestigial / no regression") — the `substrate ../nous` migration is *required + edge-preserving*; (2) `go list -m all` is a third discovery channel weave explicitly won't consult (verified zero edges today). M1 step 9 + dep spec updated accordingly.

### 2026-06-14 — M2 part 3: golden-diff harness (TDD)

**Design.** A PURE divergence classifier (`cmd/weave/internal/golden/golden.go`) + an IO gatherer (`gather.go`) + ledger formatter (`render.go`), wired as `weave golden [repoPath...]` (`cmd/weave/main.go`). The classifier (ARCH-PURE: takes a snapshot of observed live state, never touches disk; unit-tested mock-free with synthetic action-lists) classifies each weave `[]plan.Action` + the deferred-verb intents into:
  - **MATCH** — Symlink whose live link == weave's computed relative target; Mkdir whose dir exists; Touch whose target merely exists (create-if-missing); WriteFile whose content matches.
  - **EXPECTED** — the deferred ledger: `seed` (content-copy deferred), `merge`→settings.json (M4), `tool`→go.mod (deferred). setup.sh produced these; weave defers; the ledger shrinks as they land.
  - **UNEXPECTED** — anything else → harness exits non-zero.
The gatherer is STRICTLY read-only on live repos (Lstat/Readlink/Stat/ReadFile only); the subcommand runs walk→Plan with NO apply, observes, classifies, prints a per-repo ledger, exits non-zero on any UNEXPECTED. Auto-discovers present siblings (ariadne/nous/brain/metis) when no args; skip-if-absent.

**EXPECTED-divergence ledger encoded** (pre-registered, per plan Revisions): `seed bootstrap.sh`, `seed .github/workflows/merge-check.yml`, `merge .claude/settings.json` (M4), `tool cmd/sdlc` (go.mod). `AGENTS.md` is still `symlink AGENTS.md` in the live manifest (the prose flip is M5), so it MATCHES as a symlink today — correctly NOT in the EXPECTED ledger.

**Root-cause fix the harness surfaced (in-scope, not a deferred verb):** weave's `Touch` lowering emitted `WriteFile{content:""}`, which `Apply` writes UNCONDITIONALLY — it would CLOBBER the accumulated `workshop/lessons.md`. Faithful port of setup.sh:347 is create-if-missing. Added a distinct `plan.Touch` action + `applyTouch` (no-op when the file exists, any content) + classifier MATCH-on-existence. After the fix, `lessons.md` MATCHES across all repos.

**ACTUAL ledger (`weave golden` against live repos, post-Touch-fix):**
```
ariadne (self-walk): MATCH 0  EXPECTED 2  UNEXPECTED 0   (self-ref filter drops its own originals; only deferred merge/tool ledgered)
metis:               MATCH 53 EXPECTED 4  UNEXPECTED 0   ← fully clean (freshly set up)
nous:                MATCH 50 EXPECTED 4  UNEXPECTED 6
brain:               MATCH 47 EXPECTED 4  UNEXPECTED 6
```
The 6 UNEXPECTED on nous + brain are **identical**: `.colima/{Makefile,colima.sh,vm-rc.sh,vm-setup.sh,vnc-setup.sh}` + `construct/scripts/vm-log.sh` — all "weave would link → ../ariadne/…, but nothing present in live." **Diagnosis: stale-derivative drift, NOT a weave bug.** Decisive evidence: metis (set up Jun 12 16:42, most recent) MATCHES all 6 with the exact link targets weave computes; the upstream files exist in ariadne; nous/brain simply haven't been re-run through setup.sh since the `.colima` (#94) + `vm-log.sh` manifest lines landed (the files are *entirely absent* there, not mis-linked). weave running on nous/brain would correctly CREATE these links. This is exactly the drift a golden-diff is meant to surface; it is not pre-registered (not added to the EXPECTED ledger) and would close by re-weaving those two repos (deferred to M5 cutover — live repos must not be mutated now).

**Verdict on the file-op verbs:** on every repo whose setup.sh output is current (ariadne self-walk, metis), weave's file-ops MATCH with only the EXPECTED deferred ledger and **zero UNEXPECTED**. Parity proven; the harness correctly fails on the two stale repos.

**Verification:** `go test ./cmd/weave/...` GREEN (all packages incl. new `internal/golden`); `go vet ./cmd/weave/...` clean. Harness confirmed read-only (live repos' git status unchanged by the run). NO-ACTUAL: telemetry unmeasurable (brain session drives ariadne worktree).

### 2026-06-14 — M5 Phase 1 complete (self-cutover + backend-targeting); Phase 2 next

ariadne self-cutover committed on `000095-weave` (6 commits, `ac7dc2e`..`5530a0b`): migrated `base.manifest` (`symlink AGENTS.md`→`prose`; `skill construct/{local,adapted}`; `tool cmd/weave`); deleted `setup.sh`/`merge-settings.sh`/`sync-local-skills.sh`; `AGENTS.md` git-rm-cached + gitignored (now weave-composed); `Makefile.workflow refresh` → `weave compile --target claude`. Implemented the deferred **`seed`** verb (deferred ledger now empty — every setup.sh verb lowers + classifies) + the **completeness check** `weave verify-complete` (closes the M2-review P-1 under-production blind spot — **zero** under-production, both targets). **Backend-targeting (Approach 1):** `weave compile --target <claude|codex|agy>` compiles one target per invocation (claude=`.claude/skills`+prose-only AGENTS.md; codex/agy=menu, no symlinks); bare `weave` is non-mutating (footgun closed). Design recorded in the plan's `## Revisions`. Host-side read-only verify GREEN: `golden --target claude` = MATCH 24 + the 3 intended divergences (AGENTS.md/tool-go.mod/settings — resolve post-apply); `verify-complete` = 0 under-production. **Next: Phase 2 per-repo tart passes (ariadne → parley.nvim → pair → nous → brain).** M5 milestone-close after the Phase-3 merge.

### 2026-06-14 — Phase 2: ariadne tart pass PASS (1 of 5)

ariadne (the base — self-contained, no peers in the VM) on `000095-weave`. Operator-run tart pass: `make weave` (= `weave compile --target claude`) applied cleanly + idempotent; `golden --target claude` clean; `verify-complete` 0 under-production; composed `AGENTS.md` = base + ariadne's own `AGENTS.local.md` (no `@`-import); `weave skill` serves bodies; 23 `.claude/skills` symlinks; settings + `sdlc`/`make` toolchain intact. **7/7 green.** Two issues surfaced + handled: (a) `AGENTS.md` was still git-tracked (migration added `/AGENTS.md` to `.gitignore` but never `git rm --cached`'d it, so tracking overrode the ignore) → untracked in `93b533c`; (b) settings merge is two-input (ariadne-base + repo-local), NOT topological multi-layer like prose → filed `#97` (enhancement, no consumer until metis). Next: parley.nvim — first derivative, where the ★ own-`AGENTS.local.md` test is real.

### 2026-06-14 — prose-composition bug fixed via #99 (visibility axis)

The Phase-2 prose bug (a derivative composed ariadne's `AGENTS.local.md` instead of its own — the `@AGENTS.local.md` bug reproduced one level down) is fixed in **#99** (`weave: export/internal visibility mechanism`, target `weave-composition-algebra`): weave now selects 𝒜(R) = every layer's exports ⊎ the leaf's internals, composing prose foundation-first with the leaf's internal last. ariadne's `base.manifest` is annotated `export prose AGENTS.base.md` + `internal prose AGENTS.local.md` (ariadne's local only on its own self-walk). The per-derivative `internal prose AGENTS.local.md` rows still land on each derivative's #95 branch during its tart-pass prep (parley, brain, nous, metis). Verified on a parley temp-copy: composed `AGENTS.md` carries `# Parley.nvim Local Extensions` and NOT `# Ariadne Workshop Extensions`.
