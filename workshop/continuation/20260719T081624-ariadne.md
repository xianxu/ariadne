---
type: continuation
slug: ariadne
agent: claude-opus-4-8
session_id: 1428c6df-257e-4952-a447-9188582ae56d
created: 2026-07-19T08:16:24
supersedes: ariadne
branch: main
worktree: /Users/xianxu/workspace/ariadne
issues: [000182, 000171]
---

# Continuation: ariadne

## NEXT ACTION

**Decide with the operator whether to drive `project-management-primitive`
through its own funnel now.** All THREE of its MVP-scope issues are closed and
merged (#180 model, #171 residency lift, #182 calendar estimator), so the
project's task rows are all `[x]` — but the project record itself
(`workshop/projects/project-management-primitive.md`) is still at
`status: ideation` with **empty `## PRD` and `## Estimate` sections**. Its
done-when is deliberately self-referential: the file reaches `done` via
`sdlc project close` *having been driven through every funnel transition by the
machinery it tracked the building of*. So the natural next unit of work is to
take it `ideation → defined → committed → close`:

1. **Write its `## PRD`** (goal/requirements/acceptance — the `prd-present`
   guard needs it) and **`## Estimate`** Phase-A block (`**phase-a:** <N>h`;
   the `phase-a-estimate` guard needs it). These are currently empty.
2. `sdlc project set-status --slug project-management-primitive --to defined`
   (guards: prd-present, phase-a-estimate, issues-cover-prd).
3. Set a `deadline:` then `--to committed` — this now triggers **#182's own
   calendar forecast** (dogfood: the project commits using the machinery it
   built). It has ~0 remaining (all issues done) so expect a near-immediate
   projected finish; `planned_finish` derives automatically.
4. `sdlc project close --slug project-management-primitive` — records the
   fog-factor + calendar-ledger rows, archives to
   `workshop/history/projects/`.

This is a genuine operator decision (it's their portfolio artifact and the PRD
is theirs to shape), so **ASK first** rather than authoring the PRD unprompted
— the user said "we will continue with fresh context" without answering the
"drive it through the funnel?" question I posed. If they'd rather pick up
other work, the open follow-ups are **#185** (lift roadmap out of brain — the
residency follow-up #171 filed) and **#186** (shared internal git-fixture test
package — dedupe ~12 private copies, filed at #182 close).

Run `sdlc state` on resume. The `sdlc` shell function rebuilds the binary from
source every call.

## State of play

- **On `main`, clean** except two intentional non-#182/#171 items to PRESERVE,
  never stage/commit/revert:
  - `workshop/issues/000183-fix-then-ship-needs-better-process.md` — an
    uncommitted operator modification, carried untouched across both merges
    (stashed+popped each time).
  - `workshop/issues/000184-vocabulary-binary-needs-a-list-subcommand.md` —
    untracked, not mine.
- **ariadne#171 — DONE + MERGED** (PR #101, `23d211d`). Six-milestone cross-repo
  project lift: projects live in coding-repo `workshop/projects/`, discovered
  fleet-wide at close, safe peer-write commit, `sdlc project find` /
  `resolve --kind project` / parley `gP` navigation, residency docs, four legacy
  records migrated out of brain. actual 10.02h / est 4.1h.
- **ariadne#182 — DONE + MERGED** (PR #102, `3e1327b`). Project calendar
  estimator: `sdlc project throughput --bless <span>` measures focused h/wk
  from the calibration ledger; pure `ComputeForecast`/`RenderForecast` core;
  three consumers (commit derives `planned_finish` + records the statement as
  reality-check evidence, show/status render live drift, close appends a
  `## Calendar ledger` slip row). **Informs, never blocks.** actual 3.21h / est
  2.65h (ratio 0.8×). A real baseline is blessed in brain: **110.60 h/wk over
  2026-06-22..2026-07-19** (`brain/data/life/42shots/velocity/throughput-baseline.tsv`).
- **project-management-primitive** (`workshop/projects/`) — the self-referential
  tracking project. All 3 MVP rows `[x]`; **still `status: ideation`, empty
  PRD/Estimate.** This is the NEXT ACTION's subject.

## Thread arc & user model

The session ran two full SDLC cycles autonomously after the operator approved
each (#171 "go ahead", #182 through brainstorm→plan→"go ahead"→implement). The
working user model: they want staff-bar work with every milestone genuinely
boundary-reviewed (the binary owns the review; fix Critical/Important, bundle
FIX-THEN-SHIP fixes into the close commit, never re-run milestone-close), honest
test coverage (edge cases pinned, verification-caught bugs get regression
tests — e.g. #182's relative-path vantage bug), ARCH-DRY consolidation welcomed,
and unrelated working changes (#183/#184) preserved untouched. They switched the
model to **Opus 4.8** mid-#182 (Co-Authored-By trailers say "Claude Opus 4.8
(1M context)" from the change-code commit onward; earlier #171 + early #182
commits say "Claude Fable 5"). They value the dogfood arc: #182 was built partly
to be used ON project-management-primitive. On the #182 design they steered two
forks personally — throughput = operator-blessed span not trailing window
(vacations skew), and reality-check **informs not gates** ("estimation is a
funny business... always track and inform"). They are decision-maker on the
portfolio project's PRD. Away now; asked to checkpoint for fresh context.

## Open questions

- **Drive project-management-primitive through its funnel now, or pick up
  #185/#186 (or something else)?** Posed to the operator at session end,
  unanswered — this is the NEXT ACTION's fork. The PRD authoring is theirs to
  shape; don't fabricate it.

## Artifact map

Read in this order:

1. `workshop/projects/project-management-primitive.md` — the tracking project;
   its `## Log` explains the self-referential dogfood intent and the funnel
   it's meant to traverse. Empty PRD/Estimate are the immediate gap.
2. `workshop/history/issues/000182-project-calendar-estimator.md` — the just-
   shipped issue (Spec, Done-when, the `## Revisions` inform-not-gate decision,
   Estimate, and the full `## Log` with all 3 milestone closes + FIX-THEN-SHIP
   resolutions). `workshop/plans/000182-...-plan.md` + its 4 review sidecars
   (m1/m2/m3/close) are the design + review record.
3. `atlas/workflow/sdlc-binary.md` — the "Project calendar forecast" section
   (throughput baseline, pure core, fleet assembly, the three consumers) and
   the "#171" sections above it. Current map of both landed features.
4. Code: `cmd/sdlc/internal/estimate/throughput.go` (pure span math + baseline
   codec), `cmd/sdlc/internal/project/forecast.go` (pure ComputeForecast/
   RenderForecast), `cmd/sdlc/projectforecast.go` (IO seams: ListFleetProjects,
   loadThroughputBaseline, forecastForProject, forecastLine),
   `cmd/sdlc/projectthroughput.go` (the verb), `cmd/sdlc/projectsetstatus.go`
   (commit hook + planned_finish precedence), `cmd/sdlc/projectclose.go`
   (Calendar ledger row).

## Live deliberations

- **Dogfood expectation for the commit step:** when project-management-primitive
  hits `→committed`, #182's forecast fires. All its issues are done so board
  remaining ≈ 0 → `ComputeForecast` returns the zero-remaining error → the
  commit hook's `computeCommitForecast` treats a non-errNoBaseline compute error
  as informational (`cwarn`, proceeds) and does NOT derive planned_finish. So
  the operator will likely need to pass `--planned-finish` explicitly (or the
  project genuinely has nothing left to forecast — reasonable for a project
  whose work is all done). Confirm this reads sensibly rather than surprising;
  it's an untested-but-reachable path (close review §5 flagged the
  compute-error cwarn branch as low-coverage).
- **The bless baseline in brain is real data now** (110.60 h/wk). Any close of
  project-management-primitive will read it. If re-blessing is wanted with a
  different representative span, `sdlc project throughput --bless <FROM>..<TO>`.

## Decisions & dead ends

- **#182 design forks (operator-settled 2026-07-18):** blessed-span baseline
  over trailing window; contention = committed+executing (paused listed,
  weight 0); ceiling = constant 2, warning-only not arithmetic; reality-check
  informs-never-blocks (`--reality` survives only as the no-baseline fallback).
- **Verification-caught bug (don't regress):** `forecastForProject` must
  resolve the project path to ABSOLUTE before deriving the repo vantage — a
  relative `--projects-dir` (the default) collapses to `.` and silently reads 0
  remaining. Pinned by `TestForecastForProject_RelativePathResolvesVantage`.
- **Guard-ordering (don't regress):** the derived `planned_finish` is applied to
  the doc BEFORE the guard loop in `applyProjectStatus`, because `baseline-set`
  requires it. Pinned by the commit-hook tests.
- **Workflow:** every milestone + issue close was FIX-THEN-SHIP; fixes bundled
  into the close commit with `Review-Verdict:`/`Review-Window:` trailers, no
  re-run of milestone-close/close.
- **parley `gP` buffer-fallback** (from the earlier #171 Q&A) sits on parley's
  `000195-reconcile-semantic-folds-exactly` branch (commit `a5f5c0a`), NOT main
  — reaches parley main when #195 merges. Operator was offered a cherry-pick to
  main and didn't take it.

## Lessons learned

- **Live verification catches what absolute-temp-path unit tests miss.** #182's
  vantage bug only appeared when `sdlc project show` ran against the real fleet
  with the default relative projects-dir; every unit test used absolute
  `t.TempDir()` paths and passed. Run the real verb, not just the suite.
- **The atlas gate enforces per-milestone mapping.** M2's close refused to let
  me defer the atlas section to M3 (the plan had scheduled it there) — the gate
  is right, AGENTS §8 wants per-milestone atlas. Map surface at the boundary
  that introduces it.
- **Stale-binary evidence (from #171 M6, still live):** capture verification
  from a binary built from the code under review; `/tmp/claude/sdlc` goes stale
  the moment a FIX-THEN-SHIP bundle amends behavior. Rebuild before smoke-testing
  (or use the `sdlc` shell function which rebuilds each call). Already in
  `workshop/lessons.md`.
- **Milestone-close boundary reviews run Bash-broken** (EPERM on
  `~/.claude/session-env`) and drop to static analysis, demanding the MAIN agent
  run `go build ./... && go test ./cmd/sdlc/... ./pkg/vocab/` and record green in
  the close bundle. Always do it.
