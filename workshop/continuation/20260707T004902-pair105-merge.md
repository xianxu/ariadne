---
type: continuation
slug: pair105-merge
agent: claude
session_id: 2f298a28-747d-4f6b-aea4-cfe80e0706ed
created: 2026-07-07T00:49:02
supersedes: pair105-restart
branch: main
worktree: /Users/xianxu/workspace/ariadne
issues: [158, 159]
---

# Continuation: pair105-merge

## NEXT ACTION

**If you're reading this in a fresh restarted session, the Alt+Shift+C restart finally completed** — this time by running the `pair continuation` writer **unsandboxed** (`dangerouslyDisableSandbox`), which confirms the whole compaction flow (proc-walk detection + `zellij kill-session`) works outside the agent sandbox. **The sandbox was the sole blocker.** #105's `PAIR_FAKE_IN_ZELLIJ` fix handles the proc-walk half when the agent IS sandboxed; the `kill-session` step still needs the agent's shell to reach zellij's server socket.

Ship pair#105:
1. Add the kill-session/sandbox finding to `~/workspace/pair` issue **#105** `## Revisions` (it's captured in these continuations + the prior session, but not yet in the issue).
2. `cd ~/workspace/pair` (branch `000105-altc-deterministic-restart-draft-fold`), `sdlc pr` → `sdlc merge` → done.
3. Decide the sandbox question: for a *sandboxed* agent's Alt+Shift+C to complete the kill, either allowlist zellij's socket (`/sandbox`) or make pair's kill sandbox-robust. If unresolved, file a follow-up — #105's detection fix ships regardless.

## State of play
- **pair#105** — codecomplete, SHIP-reviewed, `actual 1.51h`, branch `000105-…` in `~/workspace/pair`, **NOT merged**. Detection fix (`PAIR_FAKE_IN_ZELLIJ` via `newContinueRestartCmd`) verified (re-smoke: "compacting pair-ariadne — parking scrollback…" = compaction fired under sandbox). `go test ./...` green. `sdlc state` in ~/workspace/pair for detail.
- **ariadne#158** done + shipped; **ariadne#159** wontfix → moved to pair#105.

## Thread arc & user model
Arc: closed ariadne#158 → took ariadne#159 (alt+shift+c) → moved to pair#105 → deterministic writer-owned restart + draft fold → dogfood smoke found the REAL root cause (agent sandbox blocks `InZellijPane`'s proc-walk → `pair continue` misfired, deeper than "agent skips step 2") → fixed with `PAIR_FAKE_IN_ZELLIJ` → re-closed (atlas shadow-sweep) → re-smoke: detection fixed but `kill-session` couldn't reach the server from the sandbox → **completed the restart by running unsandboxed** (isolating the sandbox as the sole cause).
User model: terse, high-trust, verify-before-ship, **dogfoods own tooling** — hit Alt+Shift+C repeatedly wanting the restart to genuinely work, which is exactly what surfaced both sandbox layers. Wants determinism + root-cause fixes.

## Open questions
On resume, resolve these open questions with the user before continuing with the NEXT ACTION.
- Sandbox policy for the agent's compaction: allowlist zellij's socket, or make pair's kill sandbox-robust? (The unsandboxed run confirmed the flow itself is correct — this is a policy/packaging decision, not a #105 code bug.)

## Artifact map
- **pair#105**, under `~/workspace/pair`: issue `workshop/issues/000105-altc-deterministic-restart-draft-fold.md` (read **Revisions** — sandbox root cause + `PAIR_FAKE_IN_ZELLIJ` fix; **add the kill-session finding**), durable plan + close-review sidecar in `workshop/plans/000105-…`. Code: `cmd/internal/continuationcmd/{draft.go,continuationcmd.go}`, `nvim/init.lua`, `atlas/architecture.md`.
- Continuation chain (in `~/workspace/ariadne/workshop/continuation/`): `pair105-ship` → `-v2` → `pair105-restart` → `pair105-merge`.

## Lessons learned
- Dogfooding a self-restarting feature surfaced TWO sandbox obstructions unit tests can't see: (1) `InZellijPane`'s process-ancestry walk is blocked (process introspection → EPERM) — fixed by preferring the env tag-match and faking the ancestry half; (2) `zellij kill-session` couldn't reach the server socket from the agent's sandbox. A feature that runs privileged/socket ops from the **agent's** shell inherits the agent's sandbox limits — the unsandboxed run isolated the sandbox as the sole cause. Widen the sandbox or make the mechanism sandbox-robust.
- Shadow-sweep ALL consumers on a single-source change (the atlas had three restatements of the compaction flow; the first close missed two + a `PAIR_FAKE_IN_ZELLIJ` "test-only" label that became production).
