---
type: continuation
slug: pair105-restart
agent: claude
session_id: 2f298a28-747d-4f6b-aea4-cfe80e0706ed
created: 2026-07-07T00:08:11
supersedes: pair105-ship-v2
branch: main
worktree: /Users/xianxu/workspace/ariadne
issues: [158, 159]
---

# Continuation: pair105-restart

## NEXT ACTION

**pair#105 is codecomplete + SHIP-reviewed and its detection fix is verified — ONE open question blocks merge, and answering it is a terminal test only you can run.**

This doc was written with `pair continuation --no-restart` on purpose: the writer-triggered restart runs `zellij kill-session` from the **agent's sandboxed shell**, and in the re-smoke that step failed (`No session named "pair-ariadne" found` even though `zellij list-sessions` sees it — the sandbox appears to block the zellij server *socket*, while list reads off disk). So the agent can't reliably complete the restart from inside the sandbox.

From a **non-sandboxed `~/workspace/ariadne` terminal**:
1. First, add the kill-session finding to `~/workspace/pair` issue **#105** `## Revisions` — it's captured here and in this session, but NOT yet in the issue.
2. Run `pair continue pair105-restart` — restarts THIS session seeded from this doc **and** tests whether `kill-session` works outside the agent sandbox.
   - **Restarts cleanly** → #105's restart works in a real terminal; the only gap is the agent sandbox. Merge: `cd ~/workspace/pair`, `sdlc pr` → `sdlc merge`. Then decide whether the agent's Alt+Shift+C needs zellij's socket allowlisted in the sandbox (`/sandbox`) — otherwise a *sandboxed* agent's writer still can't complete the kill.
   - **kill-session fails even from the terminal** → the restart needs a sandbox/socket-robust kill (new issue); do NOT merge the restart claim yet.

## State of play
- **pair#105** — codecomplete, `actual 1.51h`, branch `000105-altc-deterministic-restart-draft-fold` in `~/workspace/pair`, re-reviewed FIX-THEN-SHIP (findings fixed: atlas shadow-sweep + nvim comment). **NOT merged.** Detection fix (`PAIR_FAKE_IN_ZELLIJ` on the writer's exec, `newContinueRestartCmd`) **VERIFIED**: the re-smoke printed "compacting pair-ariadne — parking scrollback…" = compaction fired under the sandbox (the proc-walk EPERM that broke the first smoke is bypassed). Only the final `zellij kill-session` is unconfirmed outside the agent sandbox. `go test ./...` green.
- **ariadne#158** done + shipped; **ariadne#159** wontfix → moved to pair#105.

## Thread arc & user model
Arc: closed ariadne#158 → took ariadne#159 (alt+shift+c) → moved to pair#105 → deterministic writer-owned restart + draft fold → live smoke (dogfood) found the REAL root cause: the agent sandbox blocks `InZellijPane`'s proc-ancestry walk → `pair continue` misfired (deeper than "agent skips step 2") → fixed with `PAIR_FAKE_IN_ZELLIJ` → re-closed (atlas shadow-sweep) → re-smoke: detection fixed (compaction fires) but `kill-session` couldn't reach the session from the sandbox. **Two layers of sandbox obstruction, surfaced only by dogfooding.**
User model: terse, high-trust, verify-before-ship (chose "test from my terminal first" twice), dogfoods own tooling — exactly what surfaced both sandbox layers. Wants determinism + root-cause fixes, not patches.

## Open questions
On resume, resolve these open questions with the user before continuing with the NEXT ACTION.
- Does `zellij kill-session <own live session>` work from your **non-sandboxed** terminal? (list-sessions saw `pair-ariadne`; kill-session said "not found" — from the agent sandbox.)
- Is the agent's Alt+Shift+C **fundamentally** limited by the sandbox? The writer runs `kill-session` inside the agent's shell, so even with #105 a sandboxed agent may never complete the restart. Fix = allowlist zellij's socket in the sandbox, or make pair's kill sandbox-robust?

## Artifact map
- **pair#105**, all under `~/workspace/pair`: issue `workshop/issues/000105-altc-deterministic-restart-draft-fold.md` (read **Revisions** — the sandbox root cause + `PAIR_FAKE_IN_ZELLIJ` fix; the **kill-session finding is not there yet — add it**), durable plan + close-review sidecar under `workshop/plans/000105-…`. Code: `cmd/internal/continuationcmd/{draft.go,continuationcmd.go}`, `nvim/init.lua`, `atlas/architecture.md`.
- Continuation chain (all in `~/workspace/ariadne/workshop/continuation/`): `pair105-ship` → `pair105-ship-v2` → this (`pair105-restart`).

## Lessons learned
- Dogfooding a self-restarting feature surfaced TWO sandbox obstructions unit tests can't see: (1) `InZellijPane`'s process-ancestry walk is blocked (process introspection → EPERM) — fixed by preferring the env tag-match and faking the ancestry half; (2) the final `zellij kill-session` may not reach the server socket from the agent's sandbox. A feature that runs privileged/socket ops from the **agent's** shell inherits the agent's sandbox limits — a real run is the only way to find this; consider whether the sandbox must be widened or the mechanism made sandbox-robust.
- Shadow-sweep ALL consumers on a single-source change: the atlas had three restatements of "how compaction restarts"; the first close-review missed two, plus a `PAIR_FAKE_IN_ZELLIJ` "test-only" label that became production.
