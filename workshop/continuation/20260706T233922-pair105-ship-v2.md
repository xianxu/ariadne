---
type: continuation
slug: pair105-ship-v2
agent: claude
session_id: 2f298a28-747d-4f6b-aea4-cfe80e0706ed
created: 2026-07-06T23:39:22
supersedes: pair105-ship
branch: main
worktree: /Users/xianxu/workspace/ariadne
issues: [158, 159]
---

# Continuation: pair105-ship-v2

## NEXT ACTION

**You just restarted via the FIXED pair#105 writer-triggered compaction — this restart working IS the re-smoke passing** (the `PAIR_FAKE_IN_ZELLIJ` fix for the sandbox-blocked `InZellijPane` proc-walk). pair#105 is **codecomplete**, re-reviewed FIX-THEN-SHIP with all findings addressed. Ship it:

1. `cd ~/workspace/pair` (branch `000105-altc-deterministic-restart-draft-fold`). Run `sdlc pr` then `sdlc merge` → flips pair#105 → done + archives.
2. The proof is that you're reading this in a fresh session, seeded from this doc, under `pair-ariadne` with pair-dev config preserved. If the restart felt off in any way, that's a #105 finding — fix on the branch before merge.

Supersedes `pair105-ship` (which predated the sandbox fix).

## State of play
- **pair#105** — codecomplete, `actual 1.51h`, branch `000105-…` in `~/workspace/pair`, re-reviewed FIX-THEN-SHIP (atlas shadow-sweep + nvim comment fixed). **NOT merged.** Run `sdlc state` in `~/workspace/pair`.
- **ariadne#158** — done + shipped. **ariadne#159** — wontfix → moved to pair#105.

## Thread arc & user model
Arc: closed ariadne#158 → took ariadne#159 (alt+shift+c) → moved to pair#105 → implemented deterministic writer-owned restart + draft fold → **live smoke (dogfooded on this session) exposed the REAL root cause**: the agent sandbox blocks `InZellijPane`'s process-ancestry walk, so `pair continue` misfired (deeper than "agent skips step 2" — the old agent-run path hit the same wall) → fixed by having the writer pass `PAIR_FAKE_IN_ZELLIJ=1` on its exec (it already confirmed context via the env tag-match) → re-closed → this re-smoke.
User model: terse, high-trust, workflow-disciplined; wants determinism, verify-before-ship (chose "smoke-test first"), dogfoods own tooling. The smoke catching a real bug validated that instinct.

## Open questions
On resume, resolve these open questions with the user before continuing with the NEXT ACTION.
- Did the restart land cleanly — same `pair-ariadne` tag, pair-dev config, seeded from this doc? Any roughness is a finding to fix before merge.
- Watch-item (from the review): any in-pane `pair continuation` restarts, not just Alt+Shift+C (`--no-restart` escapes). Acceptable, or narrow the trigger?

## Artifact map
- **pair#105**, all under `~/workspace/pair`: issue `workshop/issues/000105-altc-deterministic-restart-draft-fold.md` (read the **Revisions** first — the sandbox root cause + fix), durable plan + close-review sidecar under `workshop/plans/000105-…`. Code: `cmd/internal/continuationcmd/{draft.go,continuationcmd.go}` (pure entities + fold/restart wiring + `newContinueRestartCmd`), `nvim/init.lua` (single-step prompt), `atlas/architecture.md` (writer-owned compaction note).

## Lessons learned
- The original framing — "a required deterministic step in an NL prompt for the agent to run isn't deterministic; move it to code" — was right but incomplete. The live smoke revealed the **deeper** cause: **process-introspection-based detection (`InZellijPane`'s proc-walk) breaks under a sandboxed agent shell**. Prefer env-signal detection (the tag-match, no introspection) and fake the introspection half once context is confirmed. A feature that can't be unit-tested end-to-end (kills/restarts a real session) needs a real dogfood run — that's what caught this.
- **Shadow-sweep ALL consumers on a single-source change.** The atlas had three restatements of "how compaction restarts"; the first close-review missed two, plus a `PAIR_FAKE_IN_ZELLIJ` "test-only" label that became production. `grep` the whole doc for the changed term, don't just add a new paragraph.
