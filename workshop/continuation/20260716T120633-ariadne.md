---
type: continuation
slug: ariadne
agent: claude
session_id: 9a8b98c7-d5be-4e40-ac58-005d1e4ab6cb
created: 2026-07-16T12:06:33
branch: 000180-project-vocabulary-model-schematize-project-like-issue-cue-lifecycle-processes
worktree: /Users/xianxu/workspace/ariadne
issues: [000180, 000171, 000182]
---

# Continuation: ariadne

## NEXT ACTION

Finish ariadne#180's M1 `FIX-THEN-SHIP` boundary before starting M2: run
`make weave` to refresh the served/generated vocabulary (the review's one
Important finding), append the review-requested `## Revisions` entry to the
durable plan, rerun the M1 verification, then rerun
`sdlc milestone-close --issue 180 --milestone M1`. This is the next step because
the first close attempt produced a valid boundary review and staged its durable
artifacts, but the Claude API overloaded before the fix/re-close cycle completed.

## State of play

- **ariadne#180 — in flight, M1 built but boundary not yet crossed.** The feature
  branch contains the project CUE model, shared lifecycle helpers,
  `pkg/vocab.Project()`, and the kind-keyed archive layout. The issue's M1 row is
  ticked and the implementor ran `go test ./...` plus
  `construct/vocabulary/vet_test.sh` successfully. The boundary review returned
  `FIX-THEN-SHIP`; its issue-log line and review sidecar are currently uncommitted.
  Read `sdlc state` and the issue file for current lifecycle state rather than
  inferring completion from the checked M1 row.
- **ariadne#171 — working dependency in the project-management MVP.** It owns
  brain-vs-repo residency/navigation and consumes #180's completed model; no #171
  implementation was attempted in this session.
- **ariadne#182 — open MVP dependency.** It owns the effort-to-calendar estimator
  that upgrades project deadline feasibility from free-text attestation to a
  computed check. It is deliberately separate from #180 but no longer post-MVP.

The three issues are tracked together by
`workshop/projects/project-management-primitive.md`, the live guinea-pig project.

## Thread arc & user model

The thread began at the end of #180's design: a large durable plan had been
written and fresh-eyes reviewed, and the user asked to begin implementation. Two
scope corrections made during approval define the work more clearly than the
initial plan did. First, the project-management lift should manage itself from
ideation rather than defer dogfooding until the machinery exists. Second, #182's
calendar estimator belongs inside the MVP: the timeline aspect of coordinating
something higher-level and longer-running is the key distinction between a
project and an issue.

The working user model is therefore: prefer self-hosting systems whose rules are
exercised by their own development; prefer computed, calibrated facts over prose
attestation; and judge scope by whether it delivers the noun's defining semantic
difference, not whether a capability can be split into a later ticket. The user
welcomes separate issues for independently designed machinery, but does not want
that separation mistaken for removal from the product-level MVP.

## Artifact map

Read in this order:

1. `workshop/issues/000180-project-vocabulary-model-schematize-project-like-issue-cue-lifecycle-processes.md`
   is the current execution record: converged spec, estimate, M1–M5 boundary
   checklist, and the M1 build log. Its uncommitted close log records the first
   `FIX-THEN-SHIP` verdict.
2. `workshop/plans/000180-project-vocabulary-model-plan.md` is the approved
   implementation design. It connects the CUE source of truth to every consumer
   through M5. Its existing dogfood revision is stale about #182's scope and must
   be superseded, not rewritten; the boundary review also asks it to reconcile
   the absent `pkg/vocab/lifecycle_test.go` and 11 migrated archive call sites.
3. `workshop/plans/000180-project-vocabulary-model-schematize-project-like-issue-cue-lifecycle-processes-m1-review.md`
   is the uncommitted boundary review. Its only Important finding is stale local
   vocabulary materialization (`make weave`). It also preserves minor findings
   and the ARCH-DRY/ARCH-PURE/ARCH-PURPOSE shadow-sweep.
4. `workshop/projects/project-management-primitive.md` connects #180, #171, and
   #182 into the self-hosting MVP. It records both operator corrections: dogfood
   from ideation, and timeline computation as an MVP-defining capability.
5. `workshop/issues/000182-project-calendar-estimator.md` preserves the later
   estimator's seed design and why it consumes, rather than expands, #180.

Implementation lives on branch
`000180-project-vocabulary-model-schematize-project-like-issue-cue-lifecycle-processes`
in worktree `/Users/xianxu/workspace/ariadne`. M1's relevant code is
`construct/vocabulary/project.cue`, `pkg/vocab/project.go`,
`pkg/vocab/lifecycle.go`, and the archive-layout consumers reached through
`pkg/vocab.ArchiveSubdir`.

## Live deliberations

The review's cosmetic minors can be handled now if they remain cheap, or filed
separately; they do not define the M1 boundary. The pointer-returning
`ProjectModel.TransitionFor` deserves another look when M3's guard runner begins
consuming it. Do not pull M5's project-datatype prose demotion forward merely
because the current prose carries the old enum: the boundary review explicitly
accepted it as sequenced work, but ARCH-PURPOSE requires M5 to remain in #180.

## Decisions & dead ends

- Rejected: defer creation of the guinea-pig project until the model and verbs
  exist. Replaced with a live ideation-stage project instance that each milestone
  exercises; destructive close/archive testing still uses a scratch repository.
- Rejected: treat #182 as a post-MVP refinement. Its implementation remains a
  separate issue, but it is in `mvp_scope` because calendar feasibility is the
  semantic differentiator of the project noun.
- Decided: paused projects must resume before normal close; `--drop` may work from
  paused. The model deliberately has no `paused -> done` transition.
- Decided: the CUE model is authoritative; every Go/help/prose consumer must
  derive from it or be guarded against drift. M5's prose demotion is therefore a
  required consumer migration, not optional cleanup.

## Lessons learned

- A boundary command can leave useful durable artifacts in the worktree even
  when the reviewing model fails afterward. Inspect the issue diff and sidecar
  before retrying; do not assume either that the boundary completed or that all
  output was lost.
- Generated local skill material is a real served consumer even when gitignored.
  Source and embedded JSON being correct is insufficient if agents read a stale
  generated face; `make weave` belongs in this boundary's consumer sweep.
- The user's scope corrections are semantic tests: self-hosting and computed
  timeline feasibility are not polish. A plan that preserves mechanical tasks
  while missing either would satisfy the easy subset and fail ARCH-PURPOSE.
